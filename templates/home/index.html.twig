{%extends 'base.html.twig'%}
{% block title %}Click&Cook{% endblock %}
{% block body %}
</header>

    <section class="hero">
        <div class="container mt-4 pt-4">
            <div class="row align-items-center">
                <!-- Colonne de gauche pour le texte -->
                <div class="col-md-6 text-start leftCol">
                    <h2 class="display-4">Votre plat du jour<br> <span class="text-danger">Un voyage culinaire</span></h2>
                  <p class="lead">Découvrez une aventure culinaire unique avec Click-and-Cook, où des recettes savoureuses et faciles à réaliser vous attendent pour régaler vos proches à chaque repas.</p>

                {% if not app.user %}
                    <a href="{{ path('app_register') }}" class="btn btn-custom btn-lg">S'inscrire</a>
                    <p class="mt-3">Vous avez déjà un compte ? <a href="{{ path('app_login') }}" class="text-danger">Se connecter</a></p>
                {% endif %}
                </div>
                <!-- Colonne de droite pour l'image -->
                <div class="col-md-5 text-end rightCol">
                    <div class="position-relative">
                        <img src="assets/img/plateone.png" class="img-fluid rounded-circle" alt="Plat du jour">
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="text-center py-5 makeRecipe">
        <div class="container">
            <h3>Partage tes <span class="text-danger">Recettes</span></h3>
           <p>Partagez vos créations culinaires avec notre communauté ! Que vous soyez un chef expérimenté ou un amateur passionné, vos recettes inspireront les autres à cuisiner et à découvrir de nouvelles saveurs.</p>

            <a href="{{ path('recipe_add') }}" class="btn btn-custom">Créer une nouvelle recette</a>
        </div>
<section class="py-5">
    <div class="container hotrecipe travelAnim One">
        <h3 class="mb-4">Nos dernières recettes</h3>
        <div class="row row-cols-1 row-cols-md-3 g-4">
            {% for recipe in recipes %}
                <div class="col">
                    <div class="card h-100">
                        <img src="{{ asset('uploads/recipes/' ~ recipe.image) }}" class="card-img-top" alt="{{ recipe.title }}">
                        <div class="card-body">
                            <h5 class="card-title">{{ recipe.title }}</h5>
                            <p class="card-text">{{ recipe.description|slice(0, 100) ~ '...' }}</p>
                        </div>
                        <div class="card-footer d-flex justify-content-center align-items-center">
                            <a href="{{ path('recipe_view', {'id': recipe.id}) }}" class="btn btn-outline-danger">❤</a>
                        </div>
                    </div>
                </div>
            {% else %}
                <p class="text-muted">Aucune recette disponible pour le moment.</p>
            {% endfor %}
        </div>
    </div>
<section class="container my-5">
    <!-- Blog Header -->
    <div class="d-flex justify-content-between align-items-center">
        <h2>Blog</h2>
        <a href="{{ path('app_blog') }}" class="text-danger text-decoration-none fw-semibold">Voir plus</a>
    </div>
    
    <!-- Blog Cards -->
    <div class="row row-cols-1 row-cols-md-3 g-4 mt-4">
        {% for article in articles %}
            <div class="col">
                <div class="card shadow-sm h-100">
                    <img src="{{ asset('uploads/articles/' ~ article.image) }}" class="card-img-top" alt="{{ article.title }}">
                    <div class="card-body">
                        <h5 class="card-title">{{ article.title }}</h5>
                        <p class="card-text text-muted">{{ article.content|striptags|slice(0, 100) ~ '...' }}</p>
                        <a href="{{ path('blog_detail', { id: article.id }) }}" class="btn btn-outline-primary">Lire plus</a>
                    </div>
                </div>
            </div>
        {% else %}
            <p class="text-muted text-center">Aucun article disponible pour le moment.</p>
        {% endfor %}
    </div>
</section>
<!-- Section voyagez -->
<section class="travelAnim Two text-center py-5"> 
    <h2 class="mb-4">Voyagez</h2>
    <div class="row row-cols-1 row-cols-md-5 g-3 justify-content-center">
        <div class="col" x-data="{}">
            <div class="card h-100">
                <img src="{{ asset('assets/img/cuisineasiatique.jpg') }}" class="card-img-top" alt="Cuisine chinoise">
                <div class="card-body">
                    <h5 class="card-title">Cuisine chinoise</h5>
                    <p class="card-text">Explorez la diversité de la cuisine asiatique, un mélange subtil de saveurs exotiques allant des sushis frais aux plats épicés comme le curry thaï. Une invitation à un voyage gustatif inoubliable !</p>
                </div>
                <div class="mb-3 align-items-center">
                    <a href="http://localhost:8000/recipe?cuisine=6&category=" class="btn btn-outline-danger">Découvrir</a>
                </div>
            </div>
        </div>

        <div class="col" x-data="{}">
            <div class="card h-100">
                <img src="{{ asset('assets/img/cuisineitalienne.webp') }}" class="card-img-top" alt="Cuisine italienne">
                <div class="card-body">
                    <h5 class="card-title">Cuisine italienne</h5>
                    <p class="card-text">Savourez la richesse de la cuisine italienne, entre pâtes fraîches, pizzas dorées et sauces tomates parfumées. Chaque plat est un hommage à l'art culinaire de l'Italie.</p>
                </div>
                <div class="mb-3 align-items-center">
                    <a href="http://localhost:8000/recipe?cuisine=1&category=" class="btn btn-outline-danger">Découvrir</a>
                </div>
            </div>
        </div>

        <div class="col" x-data="{}">
            <div class="card h-100">
                <img src="{{ asset('assets/img/cuisinemexicaine.jpg') }}" class="card-img-top" alt="Cuisine mexicaine">
                <div class="card-body">
                    <h5 class="card-title">Cuisine mexicaine</h5>
                    <p class="card-text">Explorez les saveurs vibrantes de la cuisine mexicaine, avec ses tacos épicés, guacamole crémeux et salsas fraîches. Un véritable festival de couleurs et de goûts !</p>
                </div>
                <div class="mb-3 align-items-center">
                    <a href="http://localhost:8000/recipe?cuisine=3&category=" class="btn btn-outline-danger">Découvrir</a>
                </div>
            </div>
        </div>

        <div class="col" x-data="{}">
            <div class="card h-100 travelAnim">
                <img src="{{ asset('assets/img/cuisineindienne.jpg') }}" class="card-img-top" alt="Cuisine indienne">
                <div class="card-body">
                    <h5 class="card-title">Cuisine indienne</h5>
                    <p class="card-text">Plongez dans l'univers aromatique de la cuisine indienne, avec ses currys épicés, ses naans moelleux et ses épices exotiques qui éveillent les sens.</p>
                </div>
                <div class="mb-3 align-items-center">
                    <a href="http://localhost:8000/recipe?cuisine=5&category=" class="btn btn-outline-danger">Découvrir</a>
                </div>
            </div>
        </div>
    </div>
</section>


    <section class="hero">
  <div class="container-fluid">
    <h2 class="text-center">FAQ</h2>

    <div x-data="{ open: null }">
      <div class="accordion w-100" id="faqAccordion">
        <!-- Première question -->
        <div class=" border-0 faqLeft">
          <div class="card-header text-center" id="headingOne">
            <h5 class="mb-0">
              <button class="btn" type="button" x-on:click="open === 1 ? open = null : open = 1">
                <span x-text="open === 1 ? '- Question 1 : Est-ce gratuit ?' : '> Question 1 : Est-ce gratuit ?'"></span>
              </button>
            </h5>
          </div>

          <div id="collapseOne" class="collapse" :class="{'show': open === 1}">
            <div class="card-body text-center">
              Oui, vous pouvez consulter nos recettes gratuitement en ligne.
            </div>
          </div>
        </div>

        <!-- Deuxième question -->
        <div class=" border-0 faqRight">
          <div class="card-header text-center" id="headingTwo">
            <h5 class="mb-0">
              <button class="btn" type="button" x-on:click="open === 2 ? open = null : open = 2">
                <span x-text="open === 2 ? '- Question 2 : Quelle est la politique de confidentialité ?' : '> Question 2 : Quelle est la politique de confidentialité ?'"></span>
              </button>
            </h5>
          </div>

          <div id="collapseTwo" class="collapse" :class="{'show': open === 2}">
            <div class="card-body text-center">
              Nous respectons votre vie privée et nous engageons à protéger vos données.
            </div>
          </div>
        </div>

        <!-- Troisième question -->
        <div class=" border-0 faqLeft">
          <div class=" text-center" id="headingThree">
            <h5 class="mb-0">
              <button class="btn" type="button" x-on:click="open === 3 ? open = null : open = 3">
                <span x-text="open === 3 ? '- Question 3 : Comment puis-je contacter le support ?' : '> Question 3 : Comment puis-je contacter le support ?'"></span>
              </button>
            </h5>
          </div>

          <div id="collapseThree" class="collapse" :class="{'show': open === 3}">
            <div class="card-body text-center">
              Vous pouvez nous contacter par email à l'adresse support@example.com.
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
<section>
{% endblock %}